language: python
python:
  - "3.7"
# command to install dependencies
install:
  - pip3 install tox-travis
  - pip3 install -r requirements.txt
  - pip3 install requests
# command to run tests
script:
  - flask run &
  - python test_spell.py

